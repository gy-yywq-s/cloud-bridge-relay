{
  "_comment": "private mode with a preset static token instead of the OAuth browser flow (handy for headless watchers). The token must be listed in the relay's CREW_STATIC_TOKENS.",
  "mcpServers": {
    "crew": {
      "type": "http",
      "url": "https://<your-host>/mcp",
      "headers": {
        "Authorization": "Bearer tok-abc"
      }
    }
  }
}
